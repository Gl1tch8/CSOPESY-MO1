{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run CSOPESY",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/csopesy_mo1",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "externalConsole": true,
            "MIMode": "lldb",
            "preLaunchTask": "build-app"
        }
    ]
}
